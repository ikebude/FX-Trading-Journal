# v1.1 Real-World Scenarios — 160 Institutional-Grade Improvements

**Date:** 2026-04-18
**Author:** 13-persona audit team, each wearing a senior FX-trader hat
**Purpose:** Expand v1.1 from "fix gaps in v1.0.3" to "software a 50-year institution
would ship and that makes profitable traders choose it over Edgewonk / TradeZella /
TraderVue." Every scenario below is a *real situation* a working trader faces. Not
a feature list — a scenario list. Each one answers: *"When does the trader open
the app and what do they actually need to do?"*

> **User's seed scenario (#1):** "A connected account doesn't just see trades — it
> also sees the income and outgoings to stay balanced and reconciled with MT5
> locally." That single scenario is the spine. Every scenario below is the kind of
> thing v1.1 should absorb so the app is **1000× v1.0.3 without crashing the
> update**.

---

## How to read this

- **Categories** group scenarios by capability area, but a scenario can satisfy
  multiple personas.
- **Priority labels** (P0 = v1.1 must-have, P1 = v1.1 should-have, P2 = v1.2,
  P3 = v2.0) map directly into the Option C 28-day plan. Final scoping happens
  in the plan doc; this doc captures the *universe* to choose from.
- **Effort:** S = 1 day, M = 2-4 days, L = 1-2 weeks, XL = 2+ weeks.

---

## 1. Account as full ledger (not just a trade list) — **P0**

*The seed scenario. Foundation for everything else.*

1. **S01 — Capture deposits + withdrawals from MT5 bridge.** New `balance_operations`
   table. EA extended to emit `BALANCE`/`CREDIT`/`CHARGE`/`CORRECTION`/`BONUS` deal
   events. — P0, L
2. **S02 — Balance reconciliation check on every sync.** App computes
   `starting + Σ(deposits) − Σ(withdrawals) + Σ(trade_net_pnl) + Σ(swaps) +
   Σ(commissions) + Σ(adjustments)` and compares to broker-reported balance. Red
   banner if drift > 0.01. — P0, M
3. **S03 — Missing-trade detector.** If balance drift exists and no balance op
   explains it, flag "broker balance changed by $X on YYYY-MM-DD — no matching
   event." — P0, S
4. **S04 — Deposit/withdrawal log UI.** Account page shows full cash-flow ledger
   separate from trades. — P0, M
5. **S05 — True equity curve (Modified Dietz).** Dashboard equity line excludes
   deposits/withdrawals so curve reflects *skill*, not capital additions. — P0, M
6. **S06 — Money-weighted return (IRR).** Annualized return accounting for
   mid-period cash flows. — P1, M
7. **S07 — Time-weighted return (GIPS-compliant).** Sub-period chaining so
   deposits don't inflate measured performance. — P1, M
8. **S08 — Peak balance tracking.** Max equity ever seen per account — for
   drawdown-from-peak prop firm rules. — P0, S
9. **S09 — Deposit history chart overlay on equity curve.** Vertical lines at
   each deposit with tooltip showing amount. — P1, S
10. **S10 — Broker fee breakdown widget.** Dashboard card: swaps paid,
    commissions paid, adjustments received, net fee drag in bps/year. — P1, M

---

## 2. Prop firm compliance — **P0**

*FTMO, MFF, Topstep, The5ers, E8, FundedNext, MyForexFunds, etc. Each has its own
daily loss / max loss / min days / consistency / news trading rules. Getting any
of these wrong kills the account.*

11. **S11 — Prop firm preset library.** Dropdown: FTMO Challenge $100k, FTMO Swing,
    MFF Rapid, Topstep TCP, E8, FundedNext Stellar, etc. Each preset populates
    daily loss %, max DD %, profit target %, min days, news rules, consistency
    rules. — P0, M
12. **S12 — Daily loss circuit breaker.** When realized + floating P&L hits
    daily loss %, app shows fullscreen red modal: "DAILY LOSS BREACH — STOP
    TRADING NOW." — P0, S
13. **S13 — Pre-trade rule check.** Before accepting a manual trade entry,
    simulate: "this trade at max SL would put daily loss at X% — breach=Y/N." — P0, M
14. **S14 — News-trading-forbidden window.** Most prop firms ban trading ±2-5
    minutes around red-impact news. App must block entry (or warn) inside the
    window based on connected calendar. — P0, M
15. **S15 — Consistency rule tracker.** "No single day > 30% of total profit"
    (FTMO) etc. — widget shows which days are concentration risks. — P1, M
16. **S16 — Minimum trading days tracker.** "Must trade N days before passing" —
    countdown + tick-off per day. — P0, S
17. **S17 — Weekend/holiday close-all enforcement.** Prop rules that require
    flat by Friday close — reminder 30 minutes before session close. — P1, S
18. **S18 — Stop-loss-required check.** Many prop firms reject trades without a
    stop. Flag at entry. — P0, S
19. **S19 — Maximum lot / leverage enforcement.** Firm-specific caps. — P0, S
20. **S20 — Evaluation → Funded phase migration.** When user passes, app
    imports new funded account as a linked continuation. — P1, M
21. **S21 — Scaling plan tracker.** Funded accounts often scale at profit
    milestones — widget shows next scale-up threshold. — P2, M
22. **S22 — Payout tracker.** Log each withdrawal from funded account as a
    "payout" with screenshot for audit. — P1, S

---

## 3. Risk & position sizing — **P0/P1**

*Every trader's first mistake is position-sizing error. Second is revenge sizing.
Third is pyramiding wrong.*

23. **S23 — Risk calculator on every entry path.** Already exists as standalone
    — integrate into trade-form flow with "use this size" button. — P0, S
24. **S24 — Account-risk rule enforcement.** "Never risk > 1%/trade" as a
    settable rule with hard block. — P0, S
25. **S25 — Open-risk aggregation.** Sum of all open positions' max losses —
    banner shows "currently risking 2.4% across 3 positions." — P0, M
26. **S26 — Correlation-adjusted risk.** Two correlated longs = amplified risk.
    App computes correlated-exposure R and warns. — P1, L
27. **S27 — Pyramid calculator.** "Add to winner at +1R with 0.5× size" —
    guided tool. — P2, M
28. **S28 — Break-even trigger reminder.** Post-entry, if price hits +1R and SL
    hasn't moved to BE, nudge user. — P1, M
29. **S29 — Partial-close ladder tracker.** Planned: "TP1=1R (50%), TP2=2R
    (30%), TP3=trail (20%)." App shows realized vs planned. — P1, M
30. **S30 — Kelly criterion per setup.** Given win rate + avg R, optimal bet
    size. Advisory only. — P1, S
31. **S31 — Fractional Kelly (25% Kelly) as default sizing mode.** — P2, S
32. **S32 — Max drawdown simulator.** Monte Carlo 10k runs from current stats
    to show P(drawdown > 20%) etc. — P2, M
33. **S33 — Streak-adjusted sizing.** After 3 losses, auto-suggest smaller
    size — not forced, advisory. — P2, S
34. **S34 — Margin usage widget.** Free margin %, used margin %, margin call
    distance. Live if bridge connected. — P1, M
35. **S35 — Leverage utilization bar.** Actual leverage vs max allowed. — P1, S

---

## 4. Execution quality & broker intelligence — **P1**

*Traders don't know their broker sucks until they measure. Institutional desks
have TCA (transaction cost analysis); retail traders have nothing.*

36. **S36 — Slippage tracker per symbol.** Expected fill vs actual fill,
    distribution chart. — P1, M
37. **S37 — Spread-at-entry capture.** EA records spread at the moment of order —
    widget shows avg spread per symbol per session. — P1, M
38. **S38 — Requote rate tracker.** Count requotes per 1000 orders by broker. — P2, S
39. **S39 — Broker execution score.** Composite of slippage + spread +
    requotes — compare brokers side-by-side. — P2, M
40. **S40 — Spread alert.** If spread at entry > 3σ above normal, banner: "Wide
    spread — Asian session?" — P2, S
41. **S41 — Swap calculator.** Overnight swap cost per lot per symbol, with
    triple-swap-Wednesday awareness. — P1, S
42. **S42 — Commission model config.** Per-lot, per-notional, round-trip,
    currency — matches broker's actual billing. — P0, M
43. **S43 — Pip-value by symbol from instrument table.** Already in schema,
    enforce use (no inline math). — P0, S
44. **S44 — Session-adjusted spread baselines.** London-open spread ≠ Tokyo
    spread — widget shows per-session baselines. — P2, M
45. **S45 — Bridge heartbeat monitor.** If EA bridge silent > 5 min during
    market hours, toast: "Bridge lost — check MT5." — P1, S
46. **S46 — Server time drift alert.** Broker server time vs local time — if
    drift > 5 min, warn (important for news trades). — P2, S

---

## 5. News / event awareness — **P0/P1**

*Retail traders lose money by being unaware of high-impact news. Institutional
traders wrap their exposure around the calendar.*

47. **S47 — ForexFactory auto-sync.** Weekly calendar CSV pulled automatically
    (not manual import). — P1, M
48. **S48 — News blackout entry block.** Configurable: block entries ±N min of
    red/orange news for chosen currencies. — P0, M
49. **S49 — Open-position news warning.** "FOMC in 30 min — you have 2 open
    USD positions." — P0, M
50. **S50 — News impact tagging on trade.** Trade closed within 10 min of red
    news gets auto-tag `news_trade`. — P1, S
51. **S51 — News-trading performance breakdown.** Widget: win rate / avg R on
    news trades vs off-news trades. — P1, M
52. **S52 — Calendar overlay on chart replay.** (Phase 2 chart integration.) — P2, L
53. **S53 — Bank holiday calendar.** Thin-liquidity warnings for Japan / UK /
    US holidays. — P1, S
54. **S54 — Earnings calendar for indices.** NFLX/AAPL earnings move NASDAQ —
    warn before entry. — P2, M
55. **S55 — CPI / NFP / FOMC countdown widget.** Always-visible strip with next
    3 red events for user's traded currencies. — P1, S

---

## 6. Behavioral / discipline — **P1**

*The hard problem. Software can't fix a trader's mind, but it can surface
patterns they can't see in real time.*

56. **S56 — Revenge trade detector.** Loss + new entry < 5 min + size > avg =
    warning modal: "Are you revenge trading?" — P1, M
57. **S57 — Tilt detector.** Size increase > 50% vs avg in same session — flag. — P1, M
58. **S58 — Overtrading alert.** >N trades/day for the user's baseline — red
    banner. — P1, S
59. **S59 — Undertrading / analysis-paralysis detector.** <M trades/week when
    user's plan calls for more — gentle nudge. — P2, S
60. **S60 — Pre-trade ritual enforcement.** Configurable checklist that must be
    ticked before entry: "Market bias logged? Confluence score? News check?
    Risk check?" — P1, M
61. **S61 — Post-trade reflection requirement.** Closed trades without a note
    show up in a "reflection queue" — streak counter rewards completing. — P1, M
62. **S62 — Mood check-in.** Rate mood/energy/focus 1-5 at session start —
    correlate to P&L. — P1, S
63. **S63 — Setup discipline score.** % of trades that matched a named setup vs
    freeform "I just felt it" entries. — P1, M
64. **S64 — Rule-violation counter.** Each time user breaks their own rule,
    one-click log. Weekly summary. — P1, S
65. **S65 — Fatigue warning.** >4 hours of active trading → "Take a break"
    toast. — P2, S
66. **S66 — Time-away reminder.** "You haven't journaled in 5 days — equity is
    down 3% — review?" — P2, S
67. **S67 — Streak gamification (tasteful).** Consecutive days with journal +
    review complete. Not trade-related streaks (which encourage overtrading). — P2, S

---

## 7. Setups & strategy library — **P1**

*Best-in-class journals treat a "setup" as a first-class versioned object.
Current Ledger has `setup_name` as a free-text field — insufficient.*

68. **S68 — Setup library CRUD.** Name, description, screenshot(s), entry
    rules, SL rules, TP rules, expected R. — P1, L
69. **S69 — Setup versioning.** Rule changes bump version; performance tracked
    per version so you can see when an edge degrades. — P2, M
70. **S70 — Setup expected-value card.** Per setup: win rate, avg R, expectancy,
    profit factor, 95% CI, sample size. — P1, M
71. **S71 — Setup A/B compare.** Side-by-side two setups on the same window. — P2, S
72. **S72 — Setup deprecation alert.** Edge degradation detection: rolling
    30-trade expectancy drops below threshold → "setup may be dead." — P2, M
73. **S73 — Setup templates (community/shareable).** Export/import setup JSON. — P2, S
74. **S74 — Confluence library.** Individual confluences ("double-bottom",
    "RSI div", "news catalyst") as tags — setups compose confluences. — P1, M
75. **S75 — Setup screenshots with annotations.** Attach marked-up charts to
    setup definition. — P1, S
76. **S76 — Mistake library.** Named mistakes ("moved SL", "entered on Asian
    range", "revenge-sized") as another tag class. — P1, S

---

## 8. Analytics depth — **P1**

*Every metric a professional CTA report contains.*

77. **S77 — Sharpe ratio.** Annualized, computed from daily returns. — P1, S
78. **S78 — Sortino ratio.** Downside-only deviation. — P1, S
79. **S79 — Calmar ratio.** Return / max DD. — P1, S
80. **S80 — Recovery factor.** Net profit / max DD. — P1, S
81. **S81 — MAE / MFE per trade.** Requires bridge to record adverse/favorable
    excursion while position open. — P1, L
82. **S82 — Optimal stop analysis.** "If you had placed SL at 1.2× current SL,
    you'd have saved $X." — P2, M
83. **S83 — Optimal target analysis.** "If you'd held to 2R on all winners,
    you'd be +$Y." — P2, M
84. **S84 — Trade expectancy variance.** Not just mean E — standard error /
    95% CI so trader knows how reliable the number is. — P1, S
85. **S85 — Session × day-of-week heatmap.** Already have each; add the
    cross-product. — P2, S
86. **S86 — Spread regime × performance.** Wide-spread sessions vs tight —
    do I perform worse when spread is wide? — P2, M
87. **S87 — Volatility regime detector + performance split.** ATR percentile
    bucketing — performance in top 20% vol vs bottom 20%. — P2, M
88. **S88 — Trade duration vs outcome.** Scatter: holding time × R — is the
    trader exiting winners too early? — P1, S
89. **S89 — Time-of-day curve.** 24-hour rolling win rate + avg R. — P1, S
90. **S90 — Day-after-loss cohort.** Performance on days following a losing
    day — tilt leak detector. — P2, S

---

## 9. Multi-account / portfolio — **P1**

*Serious traders run 3-10 accounts (personal, prop, funded, VPS, demo).*

91. **S91 — Multi-account portfolio dashboard.** Consolidated equity curve
    across all linked accounts, currency-normalized. — P1, L
92. **S92 — Account grouping / tags.** "Funded", "Personal", "Test" — filter
    dashboard by group. — P1, S
93. **S93 — Currency conversion for portfolio view.** JPY account balance
    displayed in USD when portfolio view is active. — P1, M
94. **S94 — Cross-account hedge detection.** Long EURUSD on Account A +
    short EURUSD on Account B → banner: "net zero exposure — intentional?" — P2, M
95. **S95 — Account performance comparison.** Side-by-side metrics table. — P1, S
96. **S96 — Per-account risk dashboard.** Each account has its own risk rules
    and own banner. — P1, M

---

## 10. Tax & compliance — **P1/P2**

*A 50-year institution would never ship a trading tool without tax primitives.*

97. **S97 — Year-end P&L statement.** Per-account, per-tax-year, signed PDF. — P1, M
98. **S98 — Section 988 / 1256 flagging (US).** Forex vs futures tax treatment. — P2, M
99. **S99 — Wash sale detection (US).** 30-day repurchase rule — flag. — P2, M
100. **S100 — UK CGT report.** Section 104 pool calculation. — P2, L
101. **S101 — Tax-lot tracking (FIFO/LIFO/HIFO configurable).** — P2, M
102. **S102 — Bonus / credit segregation.** Taxable income vs promotional
     credit. Already addressed by balance_operations schema. — P1, S
103. **S103 — Broker statement archive.** Drag-drop monthly PDFs into archive;
     cross-reference trade list. — P1, M
104. **S104 — Attestation stamp.** Monthly "I reviewed and attest this log is
     accurate — signed" (local-only, for disputes). — P2, S
105. **S105 — Immutable audit trail with SHA-256.** Each trade mutation hashed;
     chain of hashes exportable for court. — P2, M
106. **S106 — Jurisdiction-aware tax template.** User picks US/UK/EU/AU/CA —
     default categories change. — P2, L

---

## 11. Security / privacy — **P0/P1**

*Financial data is the second-most-targeted data class after healthcare.*

107. **S107 — Zip-slip fix in restore handler.** S1 from the multi-hat audit
     — [electron/ipc/backup.ts:326-330](electron/ipc/backup.ts). Path traversal
     via malicious zip entry name. — **P0, S**
108. **S108 — Tighten CSP.** [index.html](index.html) missing `frame-ancestors
     'none'`, `connect-src 'none'`. — P0, S
109. **S109 — `setPermissionRequestHandler` deny-by-default.** Main process
     currently allows any permission request from renderer. — P0, S
110. **S110 — EXIF strip on screenshot ingest.** Currently preserved — leaks
     GPS, camera metadata. — P0, S
111. **S111 — Encrypt data folder (user-opt-in).** Uses OS keychain as KEK,
     encrypts SQLite + screenshots. — P2, L
112. **S112 — Password-on-launch.** Optional; fingerprint via Windows Hello. — P2, M
113. **S113 — Screenshot redaction default.** Auto-blur balance numbers in
     captured screenshots (OCR + mask). — P2, L
114. **S114 — SHA-256 check on auto-update.** Verify downloaded installer
     hash against GitHub release asset. — P1, M
115. **S115 — Audit log export with cryptographic seal.** — P2, M
116. **S116 — No PII in electron-log.** Rule 12 verification — automated test. — P1, S
117. **S117 — Backup encryption.** User-set passphrase for exported ZIPs. — P2, M

---

## 12. Performance / reliability — **P1**

*Institutional-grade = <2s cold start, zero crashes per 10k sessions.*

118. **S118 — Startup time < 2s target.** Measure + regress. — P1, M
119. **S119 — Dashboard incremental compute.** Cache per-trade metrics, only
     recompute deltas on new/changed trades. — P1, L
120. **S120 — Code-split routes.** Blotter / Dashboard / Importer as separate
     chunks. — P1, S
121. **S121 — SQLite PRAGMA tuning.** `cache_size`, `mmap_size`, `temp_store`. — P1, S
122. **S122 — Crash reporter (opt-in, local-first).** Write crash dump to
     `%APPDATA%\Tradebook\crashes\` — user can choose to email. — P1, M
123. **S123 — Self-diagnostic command.** `/health` surface that checks DB
     integrity, bridge status, disk space, update status, backup age. — P1, S
124. **S124 — Update rollback.** Keep N-1 installer on disk; one-click revert
     after a bad update. — P1, M
125. **S125 — DB integrity checker.** Background `PRAGMA integrity_check` once
     a week; alert on failure. — P1, S

---

## 13. UX polish — **P0/P1**

*The dealbreaker for professional adoption. Retail traders forgive ugly; pros
don't.*

126. **S126 — Symbol combobox backed by instruments table.** U1-equivalent. Current
     trade form is free-text `Input` — [TradeForm.tsx:213](src/components/trade-form/TradeForm.tsx#L213). — **P0, M**
127. **S127 — Setup dropdown backed by setup library.** Same as above for
     setup field — [TradeForm.tsx:275](src/components/trade-form/TradeForm.tsx#L275). — **P0, M**
128. **S128 — Take-profit field in trade form.** Schema supports
     `initial_target_price` but form doesn't expose it. — **P0, S**
129. **S129 — Account creation UI.** Settings page says "No accounts. Add one
     via top bar." — but there's no creation flow anywhere. — **P0, M**
130. **S130 — Account type: Live + Prop + Demo, with broker metadata.** Schema
     only has `broker` text — add server, platform, leverage, timezone, login,
     broker type. — **P0, M**
131. **S131 — Theme toggle (light/dark/system).** Currently dark-only. — P1, S
132. **S132 — Cmd-K command palette.** Quick trade entry, quick nav, quick
     search across everything. — P1, M
133. **S133 — Keyboard shortcut overlay.** `?` opens cheat sheet. — P1, S
134. **S134 — Drag-drop dashboard widget reorder.** Per-user layout persisted. — P2, L
135. **S135 — Customizable widget filters per tab.** "Morning session only"
     tab, "NFP days only" tab. — P2, M
136. **S136 — Reduced-motion respect.** `prefers-reduced-motion` honored
     globally. — P1, S
137. **S137 — Color-blind safe direction pills.** Currently color-only — add
     icon/label. — P1, S
138. **S138 — Focus trap on all modals.** Accessibility P0 for institutional
     clients. — P1, S
139. **S139 — Multi-monitor window position clamp.** Saved positions sometimes
     land off-screen. — P1, S
140. **S140 — Notes autosave.** 500ms debounce + "Saved 3s ago" indicator. — P0, S
141. **S141 — Undo/redo on trade edits.** Ctrl+Z / Ctrl+Shift+Z for the last
     10 mutations. — P2, M
142. **S142 — Bulk operations on blotter.** Select rows → bulk tag, bulk
     delete, bulk export. — P1, S
143. **S143 — Pinnable trades.** Star ⭐ a trade to revisit; "Pinned" blotter tab. — P2, S
144. **S144 — Voice memo capture.** Ctrl+Alt+V records 60s audio attached to
     open trade. — P2, M
145. **S145 — Screen-region screenshot.** Not just full-screen — drag a
     rectangle. — P2, M
146. **S146 — First-run sample data toggle.** "Load 20 demo trades so I can
     explore" — purge with one click. — P1, S

---

## 14. Institutional features — **P2/P3**

*Things that make an enterprise desk choose this over a Bloomberg terminal tab.*

147. **S147 — Multi-user read-only share.** Coach/mentor can view (not edit)
     via signed local URL (network-optional). — P3, L
148. **S148 — Trade replay mode.** Step through historical trades one at a
     time, see dashboard state as-of that date. — P3, L
149. **S149 — Strategy backtest import.** CSV of backtest results attached to
     a live strategy for forward-test vs backtest comparison. — P3, L
150. **S150 — TradingView integration (URL capture).** Paste TV chart URL —
     app captures snapshot + chart settings. — P2, M
151. **S151 — TradingView chart replay link.** One-click open chart at trade
     entry timestamp. — P3, L
152. **S152 — Plugin / scripting API.** User-authored widgets via sandboxed
     JavaScript. — P3, XL
153. **S153 — Webhook emitter.** (Optional opt-in) post trade-close events to
     user-specified endpoint. — P3, M
154. **S154 — Export to Obsidian / Notion format.** Markdown + front-matter. — P2, S
155. **S155 — Import from other journals.** Edgewonk / TradeZella / TraderVue
     CSV migration. — P1, M
156. **S156 — Coach-mode comments.** A reviewer can add comments to a trade
     (import/export comment bundle). — P3, M

---

## 15. Brand / strategic — **P0**

157. **S157 — Rename to Tradebook.** Avoid Ledger-SAS trademark conflict.
     Package name, installer, icon, docs, repo. — **P0, M**
158. **S158 — User-owned data statement in README.** Explicit "Your trades
     never leave your machine unless you export them." — P0, S
159. **S159 — Open-source license decision.** Decide MIT/Apache/AGPL/proprietary
     — affects every contributor. — P0, S
160. **S160 — Release notes in-app viewer.** `/whats-new` route shows CHANGELOG
     entries; banner after update. — P1, S

---

## 16. AI & local intelligence — **P1/P2**

*What a 2026 institutional desk would have that retail tools don't. All
local-first (ONNX runtime or llama.cpp) — no cloud, no telemetry.*

161. **S161 — Local LLM journaling assistant.** Bundled small model (Phi-3 or
     Llama 3.2 3B via llama.cpp) answers questions about the user's own journal:
     "Summarize my worst 5 trades this month." Runs on CPU, offline. — P2, L
162. **S162 — Auto-classify trade setup from screenshot.** Local CLIP-like
     embedding of each screenshot; cluster → suggest setup label. — P2, L
163. **S163 — Trade-similarity search.** "Show me 10 trades most similar to
     this one" based on symbol + session + volatility + setup. — P2, M
164. **S164 — Outcome predictor.** Given new trade's features, predict
     P(win) from the user's own historical distribution. Advisory only. — P2, M
165. **S165 — Natural-language blotter query.** Search bar accepts "losers in
     NY session during NFP week" → filter applied. — P2, M
166. **S166 — Auto-summarize weekly review.** LLM drafts the review text from
     the week's trade data; user edits. — P2, M
167. **S167 — Voice memo → transcript.** Whisper.cpp local transcription of
     trade-entry voice memos. — P2, M
168. **S168 — Bias detection in journal text.** Spot patterns like "should
     have" / "if only" → flag as counterfactual thinking. — P2, S
169. **S169 — Screenshot OCR indexing.** OCR text from attached charts so
     search hits chart captions too. — P2, M
170. **S170 — End-of-day coaching prompt.** "Today's top leak: you exited 3 of
     4 winners before 1R." Generated from stats. — P1, M

---

## 17. Market-structure / methodology tagging — **P1**

*Serious technical traders tag trades by methodology. Journals that understand
the methodology become lifetime tools.*

171. **S171 — Smart Money Concepts (SMC) tag set.** BOS (break of structure),
     CHoCH, liquidity sweep, order block, fair value gap, mitigation. — P1, M
172. **S172 — ICT concepts tag set.** OTE (optimal trade entry), FVG, breaker
     block, kill zone, silver/london open models. — P1, M
173. **S173 — Wyckoff phase tagger.** Accumulation / markup / distribution /
     markdown at entry. — P2, S
174. **S174 — Elliott wave label.** Wave count at entry (1-5, A-C). — P2, S
175. **S175 — Harmonic pattern tagger.** Gartley, Bat, Butterfly, Crab, Shark. — P2, S
176. **S176 — Supply/demand zone tag.** Zone age, strength, prior-reaction
     count. — P2, M
177. **S177 — Session-bias tag.** London breakout / NY reversal / Asian range. — P1, S
178. **S178 — Confluence count per trade.** Auto-compute confluence score
     from tags (N tags = N confluences). — P1, S

---

## 18. Chart & visual intelligence — **P2/P3**

*Capture *what the chart looked like* at entry, not just the ticket.*

179. **S179 — Multi-timeframe screenshot bundle.** Paste 1H/4H/D charts as a
     single grouped attachment. — P2, M
180. **S180 — TradingView URL auto-embed.** Paste chart URL; app fetches and
     renders inline (cache locally). — P2, M
181. **S181 — Candle replay at entry timestamp.** Fetch historical OHLC;
     render mini-chart showing price action ±30 candles of entry. — P3, L
182. **S182 — Annotated entry marker on replay chart.** Draw arrow, SL line,
     TP line on replay. — P3, M
183. **S183 — Volume profile overlay.** If data provided (CSV), show volume
     nodes on the chart around entry. — P3, L
184. **S184 — Depth-of-market snapshot.** EA captures DOM at entry ticket if
     broker exposes L2. — P3, L
185. **S185 — Chart-template archive.** Save and reapply TradingView layouts
     per setup. — P3, M

---

## 19. Broker / import expansion — **P1/P2**

*Every broker the user might use. Each import is a marketing asset.*

186. **S186 — cTrader import.** cTrader CSV/XLSX statement. — P1, M
187. **S187 — NinjaTrader export import.** NT8 performance export. — P2, M
188. **S188 — Tradovate import.** Futures broker CSV. — P2, M
189. **S189 — Interactive Brokers Flex Query.** XML/CSV from IBKR client portal. — P1, L
190. **S190 — Match-Trader (DXtrade) import.** Used by FundedNext, E8, etc. — P1, M
191. **S191 — OANDA v20 API (read-only).** Pull trade history on demand — no
     trading, no credentials stored plaintext. — P2, L
192. **S192 — Dukascopy JForex import.** JForex statement format. — P2, S
193. **S193 — MT5 strategy-tester results import.** Forward-test comparison. — P2, M
194. **S194 — Crypto exchange import (Binance/Kraken/Coinbase).** Spot + futures
     trade history CSV. — P2, M
195. **S195 — TradingView Paper Trading export.** Fresh traders' first data. — P2, S
196. **S196 — Generic FIX execution report.** Parse FIX 4.4 logs. — P3, L
197. **S197 — Broker monthly PDF reconciler.** Drop broker PDF → OCR → auto-
     compare to app's records; flag drift. — P2, L

---

## 20. Trade management precision — **P1/P2**

*Live trade management, not just post-hoc logging.*

198. **S198 — Scale-in ladder planner.** Plan 3 entries at 3 levels with
     distinct sizes; track which filled. — P2, M
199. **S199 — Scale-out ladder planner.** TP1/TP2/TP3 each with % to close +
     R target. Compare planned vs actual. — P1, M
200. **S200 — Break-even SL auto-suggest.** When trade reaches +0.8R, pop
     suggestion: "Move SL to BE?" with one-click acknowledge. — P1, S
201. **S201 — Trail-stop simulator.** Compare ATR trail vs chandelier vs
     percentage trail on historical trades. — P2, M
202. **S202 — Stop-move audit.** Every SL move logged; end-of-trade review
     shows if the move helped or hurt. — P1, S
203. **S203 — Trade-idea pending queue.** Log trade ideas that weren't taken;
     revisit outcome after. — P1, M
204. **S204 — Setup expiry.** Pending idea auto-voids after N hours if not
     triggered. — P2, S
205. **S205 — Pyramiding tracker.** Adds to winners logged; evaluates if the
     add was at a valid continuation point. — P2, M

---

## 21. Reporting & external output — **P1/P2**

*What the user sends out — to a mentor, a tax prep, a prop firm, or
themselves at year-end.*

206. **S206 — Monthly trader report PDF.** 8-page template: equity curve,
     stats, setup breakdown, mistakes, next-month plan. — P1, L
207. **S207 — Year-in-review auto-generator.** 12-month retrospective with
     top/bottom trades, regime shifts, learning milestones. — P2, L
208. **S208 — Strategy factsheet PDF.** Per-strategy one-pager (Sharpe,
     Sortino, curve, setup rules). — P2, M
209. **S209 — Social-ready trade card.** Image card (PNG) with setup name,
     R, P&L, symbol — for X/Discord share (no account balance leaked). — P2, S
210. **S210 — Prop firm submission bundle.** One-click export that prop firms
     accept: trades CSV + statement PDF + attestation. — P1, M
211. **S211 — Tax-prep CSV for accountants.** Custom-formatted CSV matching
     TurboTax/H&R Block/FreeTaxUSA imports. — P1, S
212. **S212 — Investor letter template.** For users managing third-party money. — P3, M

---

## 22. Workflow automation — **P2**

*Small automations that eliminate 5 minutes/day × 365 = 30 hours/year.*

213. **S213 — Conditional rules engine.** "If daily loss ≥ 80% of limit →
     freeze trade-form." User-configurable. — P2, M
214. **S214 — Scheduled backup to OneDrive/Dropbox.** User-chosen local folder
     that happens to be cloud-synced; app writes backup there. — P2, S
215. **S215 — End-of-session auto-export.** At session close, write CSV + PDF
     to configured folder. — P2, S
216. **S216 — Self-email daily summary.** Local SMTP submission of today's
     stats to user's own address (no Tradebook server). — P2, M
217. **S217 — Auto-archive trades older than N years.** Move to
     `archive.sqlite`; still searchable via "include archive." — P2, S
218. **S218 — Startup hook.** User script runs at app launch (sandboxed
     subset of the scripting API). — P3, M

---

## 23. Psychology & wellness — **P2**

*The hardest category. Handle carefully — never make the app feel like a
therapist. These are data-capture only.*

219. **S219 — Pre-trade anxiety slider.** 0-10 rating, one tap, correlates to
     outcome over time. — P2, S
220. **S220 — Mandatory cool-down timer after N losses.** Configurable; trade-form
     disabled for M minutes. — P2, S
221. **S221 — Daily gratitude / plan prompt.** Morning popup asks one question. — P2, S
222. **S222 — Sleep hours self-report.** Single number entered at session start. — P2, S
223. **S223 — Health-data bridge (opt-in).** Read sleep/HRV from a user-exported
     JSON (no API integrations that could phone home). — P3, M
224. **S224 — Session length enforcer.** After 4 hours active, mandatory 15-min
     break before app re-enables trade form. — P2, S
225. **S225 — Mindset streak counter.** Days in a row with mood+plan logged. — P2, S

---

## 24. Multi-strategy capital management — **P2**

*Users running 3-5 distinct strategies want capital allocation, not just
one account.*

226. **S226 — Strategy capital allocation.** Split account equity by strategy
     (virtual sub-accounts). — P2, M
227. **S227 — Strategy rebalancing.** Quarterly reset of allocation based on
     strategy performance. — P3, M
228. **S228 — Strategy correlation matrix.** Which strategies lose together? — P2, S
229. **S229 — Strategy on/off flag.** Disable underperforming strategy; trades
     auto-rejected from trade-form. — P2, S

---

## 25. Asset-class specifics — **P1/P2**

*Forex isn't only forex. A 50-year institution handles every class correctly.*

230. **S230 — Futures rollover tracking.** ES/NQ/CL contract-month awareness;
     P&L continuity across rolls. — P2, M
231. **S231 — CFD dividend adjustment.** Stock-CFD dividends credited — log
     separately from P&L. — P2, S
232. **S232 — Crypto funding rate log.** 8-hour funding payments on perps —
     separate from trade P&L. — P2, S
233. **S233 — Metals pip-size special case.** XAUUSD pip size ≠ forex pip. — P0, S
234. **S234 — Indices point-value special case.** US30 / NAS100 point values
     broker-specific — pull from instrument table. — P1, S
235. **S235 — Rollover interest attribution.** Positive/negative overnight
     interest broken out by symbol. — P2, S

---

## 26. Global / cultural — **P2**

*Making the app usable by traders worldwide is a 50-year-org default.*

236. **S236 — RTL language support.** Arabic / Hebrew layouts. — P3, L
237. **S237 — CJK (Chinese/Japanese/Korean) fonts bundled.** Trader notes can
     be in any script. — P2, S
238. **S238 — Regional holiday calendars.** China Lunar NY, Ramadan thin-vol
     periods, Golden Week — affect liquidity. — P2, S
239. **S239 — Number format per locale.** 1,234.56 vs 1.234,56 vs 1 234,56. — P2, S
240. **S240 — Date format per locale.** ISO/US/EU/JP date renderers. — P2, S

---

## 27. Post-mortem & learning — **P2**

*The category retail journals skip. Institutions spend 40% of review time here.*

241. **S241 — Blown-account autopsy mode.** When account balance hits $0 or
     prop firm violation, dedicated workflow: forensic trade review, lesson
     extraction, comparison to pre-account-blow behavior. — P2, L
242. **S242 — Drawdown root-cause analyzer.** Identify which 3 trades / which
     setup / which session caused the worst DD. — P2, M
243. **S243 — "Would I take this again?" tag.** Per trade, binary — lifetime
     retrospective of conviction. — P1, S
244. **S244 — Mistake compounding tracker.** "You made the same mistake 6
     times last quarter." — P1, S
245. **S245 — Learning milestone log.** Manual entries: "2026-03-14: realized
     I exit winners too fast." Retrospectable. — P2, S

---

## Summary by priority

| Priority | Count | Effort total |
|---|---|---|
| **P0 (v1.1 must)** | 29 | ~52 dev-days |
| **P1 (v1.1 should)** | 92 | ~180 dev-days |
| **P2 (v1.2)** | 104 | ~280 dev-days |
| **P3 (v2.0)** | 20 | ~110 dev-days |
| **Total** | **245** | |

**Option C (28 calendar days) accommodates all P0 + roughly 1/3 of P1** given
the expanded P1 pool. The v1.1 plan doc will pick the exact P1 cuts based on
dependency graph — e.g., S01/S02/S05 (balance-ledger foundation) unlock
S06/S07/S08/S09, so those ship together.

## Cross-cutting observations

- **20+ scenarios depend on the balance_operations table (S01-S10, S25, S91-S96,
  S97, S102).** This is the single biggest architectural dependency in v1.1.
  Ship it week 1.
- **20+ scenarios depend on a real setup library (S68-S76, S60, S63, S171-S178,
  S200-S202, S208).** Ship it week 2 so analytics can index against it in
  weeks 3-4. The methodology-tag scenarios (S171-S178) ride the same model.
- **25+ scenarios depend on live bridge enhancements (S01, S14, S35, S36-S38,
  S45, S48, S49, S81, S184, S198-S202).** EA changes required — revise
  `LedgerBridge.mq5` once and ride the wave.
- **12+ scenarios depend on the news/event calendar (S47-S55, S14, S50, S51).**
  Auto-sync calendar early so downstream features have data.
- **Security P0s (S107-S110, S114) are independent.** Can ship in parallel.
- **AI/ML scenarios (S161-S170) all layer on top of existing data.** Can be
  batched into a single v1.2 "intelligence" release — no schema changes.
- **Import expansion (S186-S197) is a marketing surface.** Each new broker
  adds a distinct traffic/SEO channel. Prioritize brokers the user's own
  audience uses.
- **Post-mortem mode (S241-S245) is the category that differentiates this
  product from every retail journal on the market.** Institutions call this
  "lessons learned database." It's what turns a journal into a career tool.

---

## Next step

This spec is the *universe*. The v1.1 plan doc
(`docs/superpowers/plans/2026-04-19-v1.1-implementation.md`) will:
1. Lock the exact 28-day scope from this list.
2. Sequence tasks by dependency (balance-ledger foundation → bridge expansion →
   library objects → analytics → UX polish → security → release).
3. Write per-task subagent briefs following
   `superpowers:subagent-driven-development`.
4. Add review gates at days 7, 14, 21, 28.

**Ship v1.0.3 hotfix first** (3 files + changelog ready to commit), then land
this as the v1.1 input.
