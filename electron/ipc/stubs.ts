/**
 * Stub IPC handlers for channels implemented in later milestones.
 * Each returns a sensible empty/null response so the renderer doesn't crash.
 * Replace with real implementations as milestones land.
 */

export function registerStubHandlers(): void {
  // All milestones implemented — no stubs remaining.
}
